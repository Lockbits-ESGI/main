Task: T8 — Prometheus prod config lockbits_site job
Command: grep -A5 "lockbits_site" docker/prometheus/prometheus.prod.yml

Result:
  - job_name: "lockbits_site"
    metrics_path: "/metrics"
    static_configs:
      # On cible le container name et non le service name "site-web" pour éviter
      # le round-robin DNS de Dokploy entre prod et preprod.
      - targets: ["lockbits_web:80"]

✅ Target: lockbits_web:80 (container name for prod)
VERDICT: ✅ PASS
